#include "main.h"

/**
 * mul - a function that multiplies two integers
 * @a: first input
 * @b: second input
 * Return: multiplaction result of two  iputs
*/
int mul(int a, int b)
{
	return (a * b);
}
